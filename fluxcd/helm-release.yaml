apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: arch-suite
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: ./k8s/helm-charts/open-league
      sourceRef:
        kind: GitRepository
        name: arch-suite
      interval: 1m
  values:
    image:
      repository: arch-suite-
      tag: latest
    ingress:
      enabled: true
      hosts:
        - host: arch-suite.local
          paths:
            - path: /
              pathType: Prefix
    mongodb:
      enabled: true
    replicaCount: 2

