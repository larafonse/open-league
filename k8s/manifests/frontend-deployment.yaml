apiVersion: apps/v1
kind: Deployment
metadata:
  name: sports-league-frontend
  namespace: default
  labels:
    app: sports-league-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sports-league-frontend
  template:
    metadata:
      labels:
        app: sports-league-frontend
    spec:
      containers:
      - name: frontend
        image: sports-league-frontend:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: sports-league-frontend
  namespace: default
spec:
  selector:
    app: sports-league-frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
