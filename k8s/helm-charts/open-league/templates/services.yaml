apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-league.fullname" . }}-backend
  labels:
    {{- include "open-league.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.backendPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open-league.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-league.fullname" . }}-frontend
  labels:
    {{- include "open-league.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open-league.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
