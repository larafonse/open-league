apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "open-league.fullname" . }}-backend-config
  labels:
    {{- include "open-league.labels" . | nindent 4 }}
data:
  NODE_ENV: {{ .Values.config.nodeEnv | quote }}
  PORT: {{ .Values.config.port | quote }}
  MONGODB_URI: {{ if .Values.mongodb.enabled }}mongodb://{{ include "open-league.fullname" . }}-mongodb:27017/open-league{{ else }}mongodb://mongodb-service:27017/open-league{{ end }}
  JWT_SECRET: {{ .Values.config.jwtSecret | quote }}
  JWT_EXPIRE: {{ .Values.config.jwtExpire | quote }}
